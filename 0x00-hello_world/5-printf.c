#include <stdio.h>
/**
 * main - defines the entry point of our code
 * Return: 0 if exited successfully,and any value if otherwise.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
