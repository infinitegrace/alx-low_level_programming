#include "main.h"

/**
*mul - a function that returns as a result ,
*the multiplication of its arguments.
(*@a: for parameter for mul.)*
(*@b: second mul parameter.)*
*Return: result always.
*/

int mul(int a, int b)
{
	int result:

	result = a * b;

	return (result);
}
